---
name: "docker-build-cache-mounts"
description: "Guidelines and instructions for Docker build cache mounts"
---

# Docker build cache mounts Skill

## When to activate

- Applying or designing workflows requiring Docker build cache mounts.
- Implementing code patterns, structures, or configurations related to Docker build cache mounts.

## When NOT to use

- Trivial scripts or unrelated domains where Docker build cache mounts is not applicable.

## Instructions

1. Identify project requirements matching Docker build cache mounts.
2. Ensure consistent structure and clean architecture patterns are followed.
3. Validate inputs, configurations, and outputs carefully.

## Example

```typescript
// Implement or trigger Docker build cache mounts rules here
```
