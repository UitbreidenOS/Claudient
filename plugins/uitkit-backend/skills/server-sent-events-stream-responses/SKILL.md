---
name: "server-sent-events-stream-responses"
description: "Guidelines and instructions for Server-Sent Events stream responses"
---

# Server-Sent Events stream responses Skill

## When to activate

- Applying or designing workflows requiring Server-Sent Events stream responses.
- Implementing code patterns, structures, or configurations related to Server-Sent Events stream responses.

## When NOT to use

- Trivial scripts or unrelated domains where Server-Sent Events stream responses is not applicable.

## Instructions

1. Identify project requirements matching Server-Sent Events stream responses.
2. Ensure consistent structure and clean architecture patterns are followed.
3. Validate inputs, configurations, and outputs carefully.

## Example

```typescript
// Implement or trigger Server-Sent Events stream responses rules here
```
