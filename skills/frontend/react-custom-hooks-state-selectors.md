---
name: react-custom-hooks-state-selectors
description: "Guidelines and instructions for React custom hooks state selectors"
updated: 2026-06-23
---

# React custom hooks state selectors Skill

## When to activate

- Applying or designing workflows requiring React custom hooks state selectors.
- Implementing code patterns, structures, or configurations related to React custom hooks state selectors.

## When NOT to use

- Trivial scripts or unrelated domains where React custom hooks state selectors is not applicable.

## Instructions

1. Identify project requirements matching React custom hooks state selectors.
2. Ensure consistent structure and clean architecture patterns are followed.
3. Validate inputs, configurations, and outputs carefully.

## Example

```typescript
// Implement or trigger React custom hooks state selectors rules here
```
