---
name: llm-response-schema-validation-json-schema
description: "Guidelines and instructions for LLM response schema validation (JSON Schema)"
updated: 2026-06-23
---

# LLM response schema validation (JSON Schema) Skill

## When to activate

- Applying or designing workflows requiring LLM response schema validation (JSON Schema).
- Implementing code patterns, structures, or configurations related to LLM response schema validation (JSON Schema).

## When NOT to use

- Trivial scripts or unrelated domains where LLM response schema validation (JSON Schema) is not applicable.

## Instructions

1. Identify project requirements matching LLM response schema validation (JSON Schema).
2. Ensure consistent structure and clean architecture patterns are followed.
3. Validate inputs, configurations, and outputs carefully.

## Example

```typescript
// Implement or trigger LLM response schema validation (JSON Schema) rules here
```
